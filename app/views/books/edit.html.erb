<h2>Editing Book</h2>

<%= form_for(@book) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>

<% end %>

    <span>
    <%= link_to "Show", :back %>
    <%= link_to "Back", books_path %>
    </span>